[package]
name = "fsqlite-wal"
version = "0.3.14"
description = "Write-ahead logging"
edition.workspace = true
license-file.workspace = true
repository.workspace = true

[features]
default = ["native"]
native = ["fsqlite-types/native", "fsqlite-vfs/native"]
wasm = ["fsqlite-types/wasm", "fsqlite-vfs/wasm"]
fault-injection = []

[dependencies]
fsqlite-types = { workspace = true }
fsqlite-error = { workspace = true }
fsqlite-vfs = { workspace = true, default-features = false }
# bd-zywqc.11.1: mirror WAL fsync latency + pending-frame count into the global
# SLO metrics registry (observability depends only on fsqlite-types, so this
# adds no dependency cycle).
fsqlite-observability = { workspace = true }
xxhash-rust = { workspace = true }
crc32c = { workspace = true }
blake3 = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
asupersync = { workspace = true }
parking_lot = { workspace = true }
rand = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
# The final browser package owns canonical asupersync profile selection.
asupersync = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
criterion = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true

[[bench]]
name = "raptorq_matrix"
harness = false

[[test]]
name = "bd_db300_7_2_3_recovery_invariants"
required-features = ["fault-injection"]
