# Database
*.db
*.db-shm
*.db-wal
*.db.schema-migration-*
*.db-fsqlite-ns-gate
*.db-fsqlite-ns-use
*-fsqlite-ns-gate
*-fsqlite-ns-use

# WAL certification sidecars (`*.db-wal` does not match these)
*.db-wal-cert
*.db-wal-cert-head

# Lock files
*.lock

# Temporary
last-touched
*.tmp

# br CLI history snapshots (large + ephemeral)
.br_history/
.write.lock

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
